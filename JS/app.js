/* temp code for part one assignment*/


/*first function = change page to dark mode*/

/*function theme_changer(takes in switch_name) {
    if (switch_name clicked)
    doc - (set to dark mode)
    else 
    - keep page light mode
}
*/

/* second function = save button*/

/* function save_note ( take in the text) {
    if save button is clicked
    then save text 
    (not sure if it should save into an array? or into a value?)
} */

/* third function = create new note*/

/* function create_new {
    if create button pressed
    then text area will pop up 
} */
